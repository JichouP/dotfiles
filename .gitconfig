[alias]
  lg = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)%n''          %C(white)%s%C(reset) %C(dim white)- %an%C(reset)' --all
  st = status
  co = checkout
  ps = push
  pl = pull
  c = commit
  cm = commit -m
  a = add --all
  f = fetch --prune
  b = branch
  r = reset
[user]
	email = akkunn100@gmail.com
	name = JichouP
	signingkey = F47C135566613333
[core]
	editor = vim
[push]
	default = simple
[commit]
	gpgsign = true
[gpg]
	program = gpg
